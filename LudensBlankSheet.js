const prompt = require('prompt-sync')();
console.clear()

funcion = (firstPromt, secondPromt) => {

    return null
}

const firstPromt = prompt("Introduce los valores.");
const secondPromt = prompt("Introduce los valores. ");

console.log(funcion(firstPromt, secondPromt))